{
    "version": "0.2.0",
    "configurations": [{
        "name": "Launch Locally",
        "type": "go",
        "request": "launch",
        "mode": "auto",
        "program": "${workspaceRoot}/cmd/manager/",
        "env": {},
        "args": [
            "--kubeconfig",
            "${env:HOME}/.kube/config"
        ]
    }]
}